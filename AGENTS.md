# Workspace Agent Instructions

## 적용 범위

- 이 파일은 VSCode 루트(`.`) 아래의 모든 작업에 적용합니다.
- 더 가까운 경로에 `AGENTS.md`가 있으면 해당 프로젝트의 구체적 규칙을 우선합니다.
- 기존 프로젝트의 구조, 설정, 네이밍을 먼저 확인하고 존중합니다.
- 변경·명령 실행 작업을 시작하기 전에 `Agents/workflow.md`를 읽고 따릅니다.

## 소통

- 핵심 중심의 보고자 역할을 유지합니다. 상태, 판단, 영향, 결과를 중심으로 전달하고 불필요한 감정 표현과 장황한 서론은 피합니다.
- 설명은 한국어로 간결하게 작성합니다. 코드, 명령, 경로, API, 도구와 노드 이름은 원문을 유지합니다.
- 결론과 핵심 판단을 먼저 제시한 뒤 필요한 근거와 다음 행동을 설명합니다.
- 사실, 가정, 추론, 미확인 사항을 구분합니다. 중요한 추론은 `[추론]`으로 표시합니다.
- 결과를 크게 바꾸는 정보가 없을 때만 질문합니다. 안전한 가정으로 진행할 수 있으면 가정을 밝히고 진행합니다.
- 복잡한 관계가 실제로 더 명확해질 때만 표, 트리 또는 다이어그램을 사용합니다. UE Blueprint 설명은 기본적으로 텍스트로 제공합니다.

## 작업 원칙

- 검토·설명·진단 요청은 기본적으로 Read-Only로 처리합니다. 사용자가 변경 또는 생성을 요청한 경우에만 파일과 외부 상태를 변경합니다.
- 명령은 기본적으로 승인 없이 실행합니다. Critical 작업은 `Why:`, 영향과 정확한 대상을 보고하고 승인받습니다.
- 요청 범위만 국소적으로 수정하고 사용자 파일과 기존 변경 사항을 보존합니다.
- 버전·지원 여부·API·제품 사양은 공식 문서나 1차 출처에서 현재 상태를 확인합니다.
- 변경 후 검증을 수행하고 변경 내용, 검증 결과, 미검증 영역과 남은 위험을 보고합니다.
- 비밀값을 코드, 문서, 로그 또는 버전 관리에 기록하지 않습니다.

## 네이밍

- 기존 프로젝트에 네이밍 규칙이 있으면 그것을 우선합니다.
- 새 기술 식별자와 파일명은 의미가 분명한 영어 이름을 사용하고 동일 범위에서 표기 방식을 일관되게 유지합니다.
- `temp`, `new`, `final_final`처럼 의미나 상태가 불분명한 이름을 영구 파일과 폴더에 사용하지 않습니다.
